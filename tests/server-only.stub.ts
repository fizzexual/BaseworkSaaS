// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component. Aliased in vitest.config.ts so server modules guarded
// with `import "server-only"` remain importable in unit/integration tests.
export {};
